name: NamedEntityRecognitionTool
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/NamedEntityRecognitionTool
  original_url: https://huggingface.co/spaces/Chris4K/NamedEntityRecognitionTool

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
